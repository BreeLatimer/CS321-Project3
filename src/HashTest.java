/**
 * @author Bree Latimer
 * This is where the main class for this project is. This class tests the hash table
 * and hash object to ensure that they are working as intended.
 */
public class HashTest {
    public static void main(String[] args){
        //Parse command line arguments
        //1 - Each object contains an integer that is random
        //2 - Each object contains a long with System.currentTimeMillis()
        //3 - Takes in a list of words. With each word acting as a key.
        //Note: Data must be the same for both linear and double probing in order to create a fair test.
    }
}
